# Позиционное управление кнопками и светодиодами

[← К общему содержанию](../README.md)

**Оборудование:** Arduino Uno, 28BYJ-48, ULN2003, внешний источник 5 В (рекомендуется).  
**Библиотеки:** `Stepper`, `AccelStepper`.

### Общие технические уточнения
- Для 28BYJ-48 в учебных расчётах часто используют приблизительно **2048 шагов/оборот для полного шага** и **4096 шагов/оборот для полушага**. Реальное значение выходного вала может немного отличаться из-за передаточного отношения редуктора, поэтому точное позиционирование требует калибровки.
- ULN2003 подходит для волнового, полного и полушагового режима. Настоящий токовый микрошаговый режим с ним не реализуется.
- Двигатель лучше питать от отдельного 5 В источника; GND источника и Arduino объединить.
- Для библиотек важен порядок IN1–IN4; распространённый логический порядок для 28BYJ-48 — `IN1, IN3, IN2, IN4`.

## Цель
Освоить относительное и абсолютное позиционирование через AccelStepper.

## Подключение
- ULN2003 → D8…D11;
- Forward → D2;
- Back → D3;
- Zero → D4;
- LED зелёный/жёлтый/красный → D5/D6/D7 через 220–330 Ом.

## Задание
- Forward: `move(+100)`;
- Back: `move(-100)`;
- Zero: `moveTo(0)`;
- LED горит только пока соответствующее движение выполняется;
- выводить `currentPosition()` и `distanceToGo()`.

## Важное требование
`stepper.run()` должен вызываться максимально часто. Длинные `delay()` недопустимы.

## Уточнение нуля
Без концевого датчика 0 — только программная точка. После механического смещения или выключения абсолютная нулевая позиция неизвестна.

## Контрольные вопросы
1. Чем `move()` отличается от `moveTo()`?
2. Для чего нужен `run()`?
3. Что нужно для настоящего homing?

## Что включить в отчёт

1. Номер практической работы и номер задания.
2. Цель задания.
3. Схему подключения или таблицу соединений.
4. Краткое описание алгоритма.
5. Листинг программы с комментариями.
6. Скриншоты/фотографии результата.
7. При использовании Serial — скриншот Serial Monitor.
8. Краткий вывод по заданию.

