# Мигающий светодиод с отчетом

[← К общему содержанию](../README.md)

**Плата:** Arduino Uno/Nano (ATmega328P)  
**Среда:** Arduino IDE 2.x  
**Serial:** 9600 бод

### Общие замечания
- Для встроенного светодиода используйте `LED_BUILTIN`, а не жёстко заданный D13.
- Для неблокирующих задач применяйте `millis()`.
- Serial Monitor может добавлять `\r` и `\n`; это надо учитывать при разборе команд.
- Объект `String` допустим в учебных задачах, но на AVR следует помнить об ограниченном объёме SRAM.

## Цель
Управлять цифровым выходом и выводить состояние в Serial Monitor.

## Задание
1. Настроить `LED_BUILTIN` как `OUTPUT`.
2. Включить LED, вывести `СВЕТОДИОД ВКЛ`.
3. Через 1 с выключить LED, вывести `СВЕТОДИОД ВЫКЛ`.
4. Повторять.

## Дополнительный уровень
Сделайте второй вариант без `delay()` — на `millis()`.

## Контрольные вопросы
1. Что делают `pinMode()` и `digitalWrite()`?
2. Что означают `HIGH` и `LOW`?
3. Чем блокирующее мигание отличается от неблокирующего?

## Что включить в отчёт

1. Номер практической работы и номер задания.
2. Цель задания.
3. Схему подключения или таблицу соединений.
4. Краткое описание алгоритма.
5. Листинг программы с комментариями.
6. Скриншоты/фотографии результата.
7. При использовании Serial — скриншот Serial Monitor.
8. Краткий вывод по заданию.

